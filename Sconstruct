env = SConscript("godot-cpp/SConstruct")

env.Append(CPPPATH = ["src/"])

sources = Glob("src/*.cpp") + Glob("src/OrangeCharacter/*.cpp") + Glob("src/MyTestNode/*.cpp")

library = env.SharedLibrary("demo/bin/libMyTestNode{}{}".format(env["suffix"],env["SHLIBSUFFIX"]), source= sources)

Default(library)